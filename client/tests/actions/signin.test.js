import expect from 'expect';

describe('User Authentication', () => {
  beforeEach(() => {
  });
  describe('signinAction', () => {
    it('should dispatch SIGNIN_SUCCESSFUL action when a user supllies' +
        'valid credentials to login ',
    (done) => {
      expect(true).toEqual(true);
      done();
    });
  });
});
